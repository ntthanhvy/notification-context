import { Card as BtCard } from 'react-bootstrap';
import styled from 'styled-components';

export const StyledCard = styled(BtCard)``;

export const StyledCardHeader = styled(BtCard.Header)`
    background-color: #fff;
`;

export const StyledCardBody = styled(BtCard.Body)`
  height: auto;
`;
